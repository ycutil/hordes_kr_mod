{
  "name": "makegame",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Learning clone of hordes.io — authoritative WebSocket server + browser client. Architecture/protocol/data modeled on the real game (see README).",
  "scripts": {
    "start": "node server/server.js",
    "dev": "node server/server.js"
  }
}
